发布方式为 **用 PyInstaller 的 onedir 模式打包 exe+程序文件夹方式运行** 发布目录为 release文件夹 注意要以 云端训练<版本号>的方式命名文件夹

测试脚本 测试文件等一次性使用的文件 放入test文件夹

避免使用交互式命令窗口

得到打包命令再执行打包 不要每次修改都打包

采用模块化编程不要把业务逻辑都塞到主程序中 ，例如main.py作为纯编排层。

---

# 方案构建

在构建方案时，不要直接写入代码，方案中写需求，可以体现字段等
