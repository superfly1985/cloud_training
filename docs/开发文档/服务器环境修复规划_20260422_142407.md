# 服务器环境补充校验与修复规划

- 时间: `2026-04-22 14:24:07`
- 主机: `43.153.152.195:22`
- 固定解释器: `/root/miniforge3/bin/python3`

## 补充实测结果
### python版本
- 命令退出码: `0`
```bash
/root/miniforge3/bin/python3 --version
```
```bash
Python 3.10.11
```
### pip版本
- 命令退出码: `0`
```bash
/root/miniforge3/bin/python3 -m pip --version
```
```bash
pip 25.2 from /root/miniforge3/lib/python3.10/site-packages/pip (python 3.10)
```
### 关键包(冻结列表过滤)
- 命令退出码: `0`
```bash
/root/miniforge3/bin/python3 -m pip list --format=freeze | grep -E '^(numpy|torch|torchvision|torchaudio|tensorflow|tf-keras|onnx2tf|ai-edge-litert|onnx==|onnxsim|ultralytics|opencv-python|pyyaml|protobuf|h5py|flatbuffers|onnxruntime-gpu|sng4onnx|onnx-graphsurgeon)==' || true
```
```bash
numpy==2.1.2
torch==2.6.0+cu124
torchaudio==2.6.0+cu124
torchvision==0.21.0+cu124
```
### 关键导入测试
- 命令退出码: `1`
```bash
/root/miniforge3/bin/python3 -c "import importlib; mods=['yaml','numpy','cv2','PIL','torch','ultralytics','matplotlib','onnx','onnxsim','ai_edge_litert','tf_keras','onnx2tf','sng4onnx','onnx_graphsurgeon','onnxruntime','flatbuffers','google.protobuf','h5py','tensorflow']; import json; r={}; import traceback; from importlib import import_module; [(r.update({m:'OK'}) if (import_module(m) or True) else None) for m in mods if not r.get(m)]; print(json.dumps(r,ensure_ascii=False))"
```
```bash
[stderr]
Traceback (most recent call last):
  File "<string>", line 1, in <module>
  File "<string>", line 1, in <listcomp>
  File "/root/miniforge3/lib/python3.10/importlib/__init__.py", line 126, in import_module
    return _bootstrap._gcd_import(name[level:], package, level)
  File "<frozen importlib._bootstrap>", line 1050, in _gcd_import
  File "<frozen importlib._bootstrap>", line 1027, in _find_and_load
  File "<frozen importlib._bootstrap>", line 1004, in _find_and_load_unlocked
ModuleNotFoundError: No module named 'yaml'
```
### 系统库
- 命令退出码: `0`
```bash
dpkg -l | grep -E '^ii\s+libgl1-mesa-glx|^ii\s+libusb-1.0-0|^ii\s+libglib2.0-0' || true
```
```bash
ii  libglib2.0-0:amd64              2.64.6-1~ubuntu20.04.9            amd64        GLib library of C routines
```

## 最佳检查方案
- 固定只用 `/root/miniforge3/bin/python3`，禁止回落到 `/usr/bin/python3`。
- 检查顺序：`python --version` -> `pip --version` -> 关键包导入 -> 版本区间校验。
- 版本校验优先级：`exact_ver` > `min/max_ver`。
- 可选依赖仅记录告警，不阻断：`tflite_support`。
- 训练前门禁仅检查，不安装。

## 最佳安装方案
- Phase 1 系统库：`apt install -y libgl1-mesa-glx libglib2.0-0 libusb-1.0-0`。
- Phase 2 核心训练包：`pyyaml numpy==1.26.4 opencv-python==4.7.0.72 pillow torch ultralytics matplotlib onnx==1.16.1 onnxsim`。
- Phase 3 导出链：`ai-edge-litert tf-keras>=2.19,<2.20 onnx2tf>=1.28.8,<1.29 sng4onnx>=1.0.1 onnx_graphsurgeon>=0.3.26 onnxruntime-gpu flatbuffers protobuf>=3.20.3,<4 h5py tensorflow>=2.19,<2.20`。
- Phase 4 复检：逐包导入 + 版本区间检查，失败只打印首条异常并回滚到修复按钮。
- 不建议强制重装 `torch` 与 `numpy/opencv`，除非复检确认为版本不符或损坏。