# 核心必需包 - 最小化安装
# 使用方法: pip download -r requirements_core.txt -d Environment_package/

# PyTorch CPU版本
--index-url https://download.pytorch.org/whl/cpu
torch==2.0.1
torchvision==0.15.2
torchaudio==2.0.2

# YOLO
ultralytics==8.0.196

# 核心依赖
networkx==2.8.8
numpy
opencv-python
pillow
matplotlib
requests
tqdm
pyyaml

# PyTorch依赖 (--no-deps时需要)
filelock
jinja2
sympy
typing-extensions

# 基础工具
setuptools
wheel